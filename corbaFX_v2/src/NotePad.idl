module NotePadApp
{
  interface NotePad {
    boolean addNote(in string a, in long b);

    void addNullNote();

    boolean showNotes();

    boolean deleteNote(in long b);
    oneway void shutdown();
  };
};